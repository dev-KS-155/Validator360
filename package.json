{
  "name": "validotor360",
  "version": "1.0.0",
  "description": "Validotor360 is a robust validation package that ensures data integrity by checking required fields, such as strings, objects, arrays, numbers, and more, to ensure they are not null, undefined, or empty. It also validates specific formats for emails, passwords, and phone numbers.",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src --out-dir lib"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dev-KS-155/Validator360.git"
  },
  "keywords": [
    "validation",
    "fields",
    "required",
    "null-check",
    "empty-check",
    "custom-validation",
    "dynamic-validator"
  ],
  "author": "Krishna Sumit",
  "license": "MIT",
  "dependencies": {
    "jest": "^27.0.6"
  },
  "devDependencies": {
    "@babel/cli": "^7.25.6",
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.4"
  }
}